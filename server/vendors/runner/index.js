/**
 * 
 * @module vendors/runner
 * @description 运行程序
 * @author wing
 */

const configure =  require('./configure');
const middleware = require('./middleware');

module.exports = {
    configure,
    middleware
}